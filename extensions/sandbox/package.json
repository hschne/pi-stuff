{
  "name": "pi-extension-sandbox-hans",
  "version": "1.0.0",
  "description": "Opt-in bubblewrap sandbox with a read-only root, writable cwd+/tmp, secret env scrubbing, and sensitive-path masking",
  "type": "module",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
